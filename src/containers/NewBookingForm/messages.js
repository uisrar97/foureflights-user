/*
 * NewBookingForm Messages
 *
 * This contains all the text for the NewBookingForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NewBookingForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewBookingForm container!',
  },
});
