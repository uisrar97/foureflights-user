/*
 * BookingForm Messages
 *
 * This contains all the text for the BookingForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BookingForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookingForm container!',
  },
  Earn:{
    id:`${scope}.Earn`,
    defaultMessage:"Earn"
  }
});
