/*
 * FlightDetailsView Messages
 *
 * This contains all the text for the FlightDetailsView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FlightDetailsView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FlightDetailsView container!',
  },
});
