/*
 * FlightList Messages
 *
 * This contains all the text for the FlightList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FlightList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FlightList container!',
  },
});
