/*
 * Roomgallery Messages
 *
 * This contains all the text for the Roomgallery component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Roomgallery';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Roomgallery component!',
  },
});
