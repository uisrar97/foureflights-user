/*
 * Roomtype Messages
 *
 * This contains all the text for the Roomtype component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Roomtype';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Roomtype component!',
  },
});
