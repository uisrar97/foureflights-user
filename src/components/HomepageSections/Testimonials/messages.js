/*
 * Testimonials Messages
 *
 * This contains all the text for the Testimonials component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Testimonials';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Testimonials component!',
  },
});
