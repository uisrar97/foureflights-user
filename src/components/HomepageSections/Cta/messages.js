/*
 * Cta Messages
 *
 * This contains all the text for the Cta component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Cta';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Cta component!',
  },
});
